name: Append Sync Config

permissions:
  contents: write

on:
  workflow_dispatch:
    inputs:
      target_file:
        description: '目标工作流文件路径 (例如: .github/workflows/sync.yml)'
        required: true
        default: '.github/workflows/sync.yml'
        type: string
      new_task:
        description: '要新增的任务 (支持 JSON 对象或数组)'
        required: true
        type: string

jobs:
  append-config:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Append and Update Config
        env:
          TARGET_FILE: ${{ inputs.target_file }}
          NEW_TASK_INPUT: ${{ inputs.new_task }}
        run: |
          # 检查文件是否存在
          if [ ! -f "$TARGET_FILE" ]; then
            echo "::error:: 文件 $TARGET_FILE 不存在！"
            exit 1
          fi

          echo "▶ 正在读取并合并配置..."

          # 使用 Python 处理 JSON 合并与格式化
          python3 << 'PYTHON_SCRIPT'
          import re
          import json
          import sys

          file_path = sys.argv[1]
          new_input = sys.argv[2]

          # 1. 读取文件内容
          with open(file_path, 'r', encoding='utf-8') as f:
              content = f.read()

          # 2. 提取标记中间的内容
          # 正则说明：匹配 START 标记 -> 捕获中间内容 -> END 标记
          pattern = r"(# === SYNC_CONFIG_START ===\s*SYNC_TASKS=')(.*?)(' \s*# === SYNC_CONFIG_END ===)"
          match = re.search(pattern, content, re.DOTALL)

          if not match:
              print("::error:: 未找到配置标记。请确保目标文件包含 # === SYNC_CONFIG_START === 和 END 标记。")
              sys.exit(1)

          # 3. 解析旧配置
          old_json_str = match.group(2).strip()
          try:
              # 尝试解析旧 JSON，如果为空或无效则初始化为空列表
              if old_json_str:
                  current_list = json.loads(old_json_str)
              else:
                  current_list = []
          except json.JSONDecodeError:
              print("::error:: 原有配置 JSON 格式错误，无法解析。")
              sys.exit(1)

          # 4. 解析新输入
          try:
              new_data = json.loads(new_input)
              # 如果输入的是单个对象，转为列表
              if isinstance(new_data, dict):
                  new_items = [new_data]
              else:
                  new_items = new_data
          except json.JSONDecodeError:
              print("::error:: 输入的新配置 JSON 格式错误。")
              sys.exit(1)

          # 5. 合并配置 (旧 + 新)
          merged_list = current_list + new_items

          # 6. 格式化新 JSON (带缩进，看起来像 YAML 里的风格)
          # indent=2 生成紧凑美观的格式
          merged_json_str = json.dumps(merged_list, indent=2, ensure_ascii=False)

          # 计算缩进 (为了美观，我们需要给 JSON 的每一行加上缩进，使其对齐)
          # 这里获取 SYNC_TASKS=' 所在行的缩进
          prefix_line = match.group(1)
          indent_match = re.search(r"\n([ \t]*)", prefix_line)
          base_indent = indent_match.group(1) if indent_match else "          "

          # 对 JSON 内容进行缩进处理 (第一行不缩进，后续行缩进)
          json_lines = merged_json_str.split('\n')
          formatted_json = json_lines[0] + "\n" + "\n".join([base_indent + line for line in json_lines[1:]])

          # 7. 替换内容
          # 构造新的代码块
          new_block = f"{match.group(1)}{formatted_json}{match.group(3)}"
          new_content = content[:match.start()] + new_block + content[match.end():]

          # 8. 写回文件
          with open(file_path, 'w', encoding='utf-8') as f:
              f.write(new_content)

          print(f"✅ 配置已更新。原任务数: {len(current_list)}，新增: {len(new_items)}，总数: {len(merged_list)}")
          PYTHON_SCRIPT "$TARGET_FILE" "$NEW_TASK_INPUT"

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          if git diff --quiet; then
            echo "ℹ️ 没有检测到内容变化（可能配置已存在），无需提交。"
            exit 0
          fi
          
          git add "$TARGET_FILE"
          git commit -m "chore: append new sync task configuration"
          git push
          echo "🚀 新配置已追加并提交。"